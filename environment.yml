name: gan_experiment
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - numpy>=1.24
  - scipy>=1.11
  - openpyxl>=3.1
  - pip
  - pip:
    - pot>=0.9.0  # Python Optimal Transport (for Wasserstein distance in high-dim)
